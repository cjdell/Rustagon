# CLI Emulator

## Building

Should be as simple as:

    cargo build -r

## Running

The SDK is configured (via `.cargo/config.toml`) to automatically runs the emulator when attempting to run an example.
